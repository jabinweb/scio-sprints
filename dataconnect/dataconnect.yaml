specVersion: "v1beta"
serviceId: "scio-sprints"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "scio-sprints-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
